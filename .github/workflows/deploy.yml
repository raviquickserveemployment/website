name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true  # Allows authentication for deployment
          fetch-depth: 0  # Ensures full Git history

      - name: Inject Secret into JSON File
        run: |
          echo '{ "GITHUB_TOKEN": "${{ secrets.GH_PAT }}" }' > secret.json

      - name: Use Secret Without Committing
        run: |
          cp secret.json /tmp/secret.json  # Temporarily store outside repo

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: main
          folder: .
          clean: true
          token: ${{ secrets.GH_PAT }}  # Use the new Personal Access Token

      - name: Remove Secret File
        run: rm -f /tmp/secret.json  # Remove after use
